# 静态文件管理 - 快速参考

## 🚀 快速开始

### 启动本地开发服务器
```bash
./start-dev.sh
```

### 同步静态文件
```bash
node sync-assets.js
```

### 部署到生产环境
```bash
git add . && git commit -m "更新静态文件" && git push origin main
```

## 📁 文件结构

```
project/
├── public/               # 源文件存储 (✅ 总是编辑这里的文件)
│   ├── styles.css
│   ├── script.js
│   └── images/
├── styles.css           # 自动同步到根目录
├── script.js            # 自动同步到根目录
├── sync-assets.js       # 同步脚本
└── start-dev.sh         # 启动脚本
```

## ⚠️ 重要提醒

1. **始终编辑 `public/` 目录中的文件**
2. **修改后运行同步脚本**
3. **本地测试后再部署**

## 📖 详细文档

查看 `VERCEL_STATIC_FILES_GUIDE.md` 了解完整的规则和故障排除方法。

---

**网站地址**: https://osd.talentdual.com/
**本地开发**: http://localhost:8000/
