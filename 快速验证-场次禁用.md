# 🧪 快速验证 - 场次禁用功能

## 🚀 验证步骤

### 步骤 1：刷新页面

按 **Ctrl + Shift + R** (Windows) 或 **Cmd + Shift + R** (Mac) 强制刷新页面，清除缓存。

---

### 步骤 2：打开开发者工具

按 **F12** 打开控制台

---

### 步骤 3：执行验证脚本

在控制台中粘贴并运行以下脚本：

```javascript
console.log('========== 场次禁用功能验证 ==========\n');

// 1. 检查场次数量
const allVenues = document.querySelectorAll('input[name="selectedVenues"]');
console.log('1️⃣ 场次总数:', allVenues.length);

if (allVenues.length === 0) {
    console.error('❌ 没有找到场次元素！');
} else {
    console.log('✅ 场次元素加载成功\n');
    
    // 2. 显示所有场次的初始状态
    console.log('2️⃣ 初始状态:');
    allVenues.forEach((v, i) => {
        console.log(`场次${i+1}: ${v.value}`, {
            disabled: v.disabled,
            checked: v.checked,
            pointerEvents: v.closest('.session-option')?.style.pointerEvents || 'auto'
        });
    });
}

console.log('\n========================================');
console.log('📝 请手动测试:');
console.log('1. 点击选择第一个场次');
console.log('2. 观察其他场次是否变灰且不可点击');
console.log('3. 控制台应显示: "✅ 已选择场次，其他场次已禁用"');
console.log('4. 尝试点击其他场次（应该无反应）');
console.log('5. 取消第一个场次');
console.log('6. 控制台应显示: "✅ 已取消选择，所有场次已恢复"');
console.log('========================================\n');
```

---

### 步骤 4：手动测试

#### 测试 A：选择场次

1. **点击选择第一个场次**

**✅ 预期结果：**
- 第一个场次被选中（勾选）
- 其他所有场次立即变灰（opacity: 0.5）
- 其他场次的 checkbox 被禁用
- 控制台显示：`✅ 已选择场次，其他场次已禁用`

**❌ 如果失败：**
- 其他场次仍然可以点击
- 其他场次没有变灰

---

#### 测试 B：尝试点击其他场次

2. **尝试点击其他灰色的场次**

**✅ 预期结果：**
- 点击无任何反应
- 鼠标悬停显示"禁止"光标
- 无法勾选其他场次

**❌ 如果失败：**
- 其他场次可以被选中
- 同时选中了多个场次

---

#### 测试 C：取消选择

3. **取消第一个场次的勾选**

**✅ 预期结果：**
- 第一个场次取消选中
- 所有场次恢复正常颜色（opacity: 1）
- 所有场次重新可以选择
- 控制台显示：`✅ 已取消选择，所有场次已恢复`

**❌ 如果失败：**
- 其他场次仍然是灰色
- 其他场次仍然不可点击

---

## 🔍 调试脚本

### 脚本 1：查看场次状态

```javascript
// 查看当前所有场次的详细状态
const venues = document.querySelectorAll('input[name="selectedVenues"]');
console.log('场次状态检查:');
venues.forEach((v, i) => {
    const sessionOption = v.closest('.session-option');
    console.log(`\n场次${i+1}: ${v.value}`);
    console.log('  - checkbox.disabled:', v.disabled);
    console.log('  - checkbox.checked:', v.checked);
    console.log('  - style.opacity:', sessionOption?.style.opacity || '1');
    console.log('  - style.cursor:', sessionOption?.style.cursor || 'auto');
    console.log('  - style.pointerEvents:', sessionOption?.style.pointerEvents || 'auto');
});
```

---

### 脚本 2：模拟选择测试

```javascript
// 自动测试选择和取消
const testVenueSelection = () => {
    const venues = document.querySelectorAll('input[name="selectedVenues"]');
    
    if (venues.length < 2) {
        console.error('❌ 场次数量不足，无法测试');
        return;
    }
    
    console.log('开始自动测试...\n');
    
    // 测试1：选择第一个场次
    console.log('1️⃣ 选择第一个场次...');
    venues[0].click();
    
    setTimeout(() => {
        // 检查其他场次是否被禁用
        const otherDisabled = Array.from(venues).slice(1).every(v => v.disabled);
        console.log('其他场次是否被禁用:', otherDisabled ? '✅ 是' : '❌ 否');
        
        // 测试2：尝试选择第二个场次
        console.log('\n2️⃣ 尝试选择第二个场次...');
        venues[1].click();
        
        setTimeout(() => {
            const secondChecked = venues[1].checked;
            console.log('第二个场次是否被选中:', secondChecked ? '❌ 是（不应该）' : '✅ 否（正确）');
            
            // 测试3：取消第一个场次
            console.log('\n3️⃣ 取消第一个场次...');
            venues[0].click();
            
            setTimeout(() => {
                const allEnabled = Array.from(venues).every(v => !v.disabled);
                console.log('所有场次是否恢复:', allEnabled ? '✅ 是' : '❌ 否');
                
                console.log('\n========== 测试完成 ==========');
            }, 500);
        }, 500);
    }, 500);
};

// 运行测试
testVenueSelection();
```

---

## 📊 问题排查

### 问题 1：其他场次没有变灰

**检查：**
```javascript
// 选择第一个场次后，检查第二个场次的样式
const venue2 = document.querySelectorAll('input[name="selectedVenues"]')[1];
const sessionOption2 = venue2?.closest('.session-option');
console.log('第二个场次样式:', {
    opacity: sessionOption2?.style.opacity,
    cursor: sessionOption2?.style.cursor,
    pointerEvents: sessionOption2?.style.pointerEvents
});

// 应该显示：
// opacity: "0.5"
// cursor: "not-allowed"
// pointerEvents: "none"
```

---

### 问题 2：其他场次仍然可以点击

**检查：**
```javascript
// 检查 disabled 属性和 pointerEvents
const venue2 = document.querySelectorAll('input[name="selectedVenues"]')[1];
console.log('第二个场次可点击性:', {
    disabled: venue2.disabled,  // 应该是 true
    pointerEvents: venue2.closest('.session-option')?.style.pointerEvents  // 应该是 "none"
});
```

**如果 pointerEvents 不是 "none"：**
- 说明代码没有正确执行
- 可能需要清除浏览器缓存并强制刷新

---

### 问题 3：控制台没有显示消息

**检查：**
```javascript
// 验证事件监听器是否正确绑定
const venue1 = document.querySelectorAll('input[name="selectedVenues"]')[0];
console.log('第一个场次:', venue1);
console.log('name属性:', venue1?.name);  // 应该是 "selectedVenues"
```

---

## 🔄 强制刷新步骤

如果修改后没有生效，请执行以下步骤：

### Windows / Linux
1. 按 **Ctrl + Shift + Delete**
2. 选择"缓存的图片和文件"
3. 点击"清除数据"
4. 或者直接按 **Ctrl + Shift + R** 强制刷新

### Mac
1. 按 **Cmd + Shift + Delete**
2. 或者直接按 **Cmd + Shift + R** 强制刷新

### 或者在浏览器中
1. 打开开发者工具（F12）
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

---

## ✅ 成功的标志

如果功能正常，您应该看到：

### 视觉效果
```
选中第一个场次前：
☐ 成都 - 2026年03月15日  (正常颜色)
☐ 成都 - 2026年06月20日  (正常颜色)
☐ 北京 - 2026年09月10日  (正常颜色)

选中第一个场次后：
☑ 成都 - 2026年03月15日  (正常颜色，可操作)
☐ 成都 - 2026年06月20日  (灰色，不可点击) ← 关键
☐ 北京 - 2026年09月10日  (灰色，不可点击) ← 关键

取消第一个场次后：
☐ 成都 - 2026年03月15日  (恢复正常)
☐ 成都 - 2026年06月20日  (恢复正常)
☐ 北京 - 2026年09月10日  (恢复正常)
```

### 控制台输出
```
✅ 已选择场次，其他场次已禁用
✅ 已取消选择，所有场次已恢复
```

---

## 🎯 如果仍然不工作

请在控制台运行以下完整诊断：

```javascript
console.log('========== 完整诊断 ==========\n');

// 1. 检查 script.js 是否加载
console.log('1️⃣ 检查全局变量:');
console.log('examSessionsData:', typeof examSessionsData);
console.log('getLocationCode:', typeof getLocationCode);

// 2. 检查场次元素
const venues = document.querySelectorAll('input[name="selectedVenues"]');
console.log('\n2️⃣ 场次元素:', venues.length, '个');

// 3. 检查事件监听
console.log('\n3️⃣ 测试选择第一个场次...');
if (venues.length > 0) {
    venues[0].click();
    
    setTimeout(() => {
        console.log('选择后状态:');
        venues.forEach((v, i) => {
            if (i > 0) {
                console.log(`场次${i+1}:`, {
                    disabled: v.disabled,
                    opacity: v.closest('.session-option')?.style.opacity
                });
            }
        });
        
        // 取消选择
        venues[0].click();
    }, 1000);
}

console.log('\n========================================');
```

---

**如果问题仍然存在，请截图控制台输出并告知我！** 📸
